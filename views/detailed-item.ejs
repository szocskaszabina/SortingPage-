
<!doctype html>

  <html>
	<head>
		<title>Details</title>
		<meta charset="UTF-8">
        <meta name="viewport" content="initial-scale=1.0">
        <link rel="stylesheet" href="styles.css">
        <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css" integrity="sha384-MCw98/SFnGE8fJT3GXwEOngsV7Zt27NXFoaoApmYm81iuXoPkFOJwJ8ERdknLPMO" crossorigin="anonymous">
	</head>

	<body>
        <div class="container-fluid d-flex flex-row justify-content-around align-items-center">
        <img id="mainImage" src= "<%= item.image %>" alt="">
        <div class="content">
        <h2><%= item.name %></h2>
        <ul>Properties
            <li><%= item.type %></li>
            <li><%= item.gender %></li>
            <li><%= item.color %></li>
            <li><%= item.size %></li>
        </ul>
        <h3><%= item.price %> Huf</h3>
        <p>Available: <%= item.available %></p>
        <p><%= item.description %></p>
  
      <div class="container-fluid d-flex flex-row justify-content-around align-items-center">
        <img class="photos" src= "<%= item.image %>" alt="">
        <% for (let image of item.gallery) { %>
          <img class="photos" src="<%= image %>" alt="">
          <% } %>
        </div>
        </div>
      </div>
       <script src="item.js"></script> 
</body>
</html>